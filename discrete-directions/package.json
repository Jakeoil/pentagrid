{
  "name": "discrete-directions",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "A second geometry: penrose-mosaic's integer substitution, its limiting directions, and what a multigrid on them would be",
  "scripts": {
    "build": "tsc -p .",
    "test": "node --test tools/wheels.test.mjs"
  }
}
